import { PERMISSION_KEYS } from '../contracts/permission-keys';

/** Functional permission key from access-model.md §2.3 — gates field definition writes. */
export const MANAGE_CUSTOM_FIELDS_PERMISSION =
  PERMISSION_KEYS.MANAGE_CUSTOM_FIELDS;

export { PERMISSION_KEYS };
